---
phase: 02-planner-agent
plan: 02
type: tdd
wave: 2
depends_on:
  - 02-01
files_modified:
  - declarative_ai/agents/planner.py
  - tests/test_planner.py
autonomous: true
requirements:
  - PLAN-01
  - PLAN-05

must_haves:
  truths:
    - "run_planner() returns a PlannerOutput when given a BuildState with a spec (first iteration)"
    - "run_planner() uses Mode A prompt (full spec decomposition) when gap_report is None"
    - "run_planner() uses Mode B prompt (delta re-plan) when gap_report is not None"
    - "In Mode B, the gap report is the primary input and the spec is secondary (reference only)"
    - "run_planner() output validates against PlannerOutput schema without manual parsing"
  artifacts:
    - path: "declarative_ai/agents/planner.py"
      provides: "Planner agent with two-mode prompt strategy"
      exports: ["run_planner"]
      min_lines: 50
    - path: "tests/test_planner.py"
      provides: "Planner agent unit tests with mocked API"
      min_lines: 80
  key_links:
    - from: "declarative_ai/agents/planner.py"
      to: "declarative_ai/agents/base.py"
      via: "call_agent() for Claude API calls"
      pattern: "from declarative_ai\\.agents\\.base import call_agent"
    - from: "declarative_ai/agents/planner.py"
      to: "declarative_ai/schema/task_schema.py"
      via: "PlannerOutput as response_model"
      pattern: "from declarative_ai\\.schema\\.task_schema import PlannerOutput"
    - from: "declarative_ai/agents/planner.py"
      to: "declarative_ai/state.py"
      via: "BuildState as input parameter"
      pattern: "from declarative_ai\\.state import BuildState"
---

<objective>
Implement the Planner agent with two-mode prompt strategy: full spec decomposition on iteration 0 and delta re-planning from gap report on iteration 1+.

Purpose: The Planner is the first agent in the build loop. It transforms a declarative spec into an actionable task graph that Workers can execute. Delta re-planning (PLAN-05) prevents wasteful full re-plans on subsequent iterations.

Output: planner.py (run_planner function with Mode A and Mode B prompts) and its test suite — all green.
</objective>

<execution_context>
@C:/Users/User/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/User/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-planner-agent/02-RESEARCH.md
@.planning/phases/02-planner-agent/02-VALIDATION.md
@.planning/phases/02-planner-agent/02-01-SUMMARY.md

@declarative_ai/agents/base.py
@declarative_ai/state.py
@declarative_ai/schema/task_schema.py

<interfaces>
<!-- From declarative_ai/agents/base.py (Phase 1): -->
```python
def call_agent(
    client: anthropic.Anthropic,
    system: str,
    user_message: str,
    response_model: Type[T],
    model: str = "claude-opus-4-5",
    max_parse_retries: int = 3,
) -> T:
```

<!-- From declarative_ai/state.py (Phase 1): -->
```python
class BuildState(BaseModel):
    spec: dict[str, Any]
    status: Literal["planning", "building", "validating", "done", "failed"] = "planning"
    iteration: int = 0
    task_graph: Optional[list[dict[str, Any]]] = None
    snapshot: Optional[dict[str, Any]] = None
    gap_report: Optional[dict[str, Any]] = None
```

<!-- From declarative_ai/schema/task_schema.py (Plan 01 output): -->
```python
class TaskNode(BaseModel):
    id: str
    task_type: Literal["create_file", "modify_file", "create_test", "create_config"]
    target_file: str
    writes_files: list[str]
    description: str
    acceptance_criteria: list[str]
    dependencies: list[str]
    spec_refs: list[str] = []

class PlannerOutput(BaseModel):
    tasks: list[TaskNode]
    iteration: int
    reasoning: str = ""
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: run_planner Mode A (first iteration) with TDD</name>
  <files>declarative_ai/agents/planner.py, tests/test_planner.py</files>
  <behavior>
    - test_planner_first_iteration: When state.gap_report is None, run_planner() calls call_agent() and returns a PlannerOutput. Mock call_agent to return a fixture PlannerOutput. Verify returned object has tasks list and iteration field.
    - test_planner_output_validates: The PlannerOutput returned by run_planner (mocked) validates — it has tasks with all required TaskNode fields.
    - test_planner_mode_a_prompt_contains_spec: When gap_report is None, the user_message passed to call_agent contains the spec JSON. Capture call_agent args via mock.
    - test_planner_mode_a_prompt_no_gap_report: When gap_report is None, the user_message does NOT contain "Gap report". Capture call_agent args via mock.
    - test_planner_passes_response_model: call_agent is called with response_model=PlannerOutput. Verify via mock.
    - test_planner_accepts_explicit_client: run_planner(state, client=mock_client) passes that client to call_agent, not get_client().
  </behavior>
  <action>
    RED: Create tests/test_planner.py with all 6 behavior tests. Use pytest-mock (mocker fixture) to patch declarative_ai.agents.planner.call_agent. Create a helper that builds a fixture PlannerOutput with 2 TaskNodes. Create a helper that builds a minimal BuildState (spec=simple dict, gap_report=None, iteration=0). Run tests — all must FAIL.

    GREEN: Create declarative_ai/agents/planner.py with:
    - PLANNER_SYSTEM_PROMPT_V0: System prompt for Mode A (full decomposition). Must instruct: decompose spec into minimal task set, one file per task, set writes_files to ALL files touched, use task-{type}-{slug} ID convention, output valid JSON matching PlannerOutput schema.
    - run_planner(state: BuildState, client: anthropic.Anthropic | None = None) -> PlannerOutput
    - In Mode A (state.gap_report is None): serialize state.spec with json.dumps, pass as user message, call call_agent with system=PLANNER_SYSTEM_PROMPT_V0, response_model=PlannerOutput
    - If client is None, call get_client()

    Run tests — all must PASS.

    REFACTOR: Extract user message building into _build_user_message_v0(spec: dict) helper. Add docstrings referencing PLAN-01.
  </action>
  <verify>
    <automated>cd C:/Users/User/Desktop/declerative-ai && python -m pytest tests/test_planner.py -x -v -k "not delta"</automated>
  </verify>
  <done>run_planner returns PlannerOutput on first iteration, uses Mode A prompt with spec JSON, passes PlannerOutput as response_model to call_agent — 6 tests green</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: run_planner Mode B (delta re-plan from gap report) with TDD</name>
  <files>declarative_ai/agents/planner.py, tests/test_planner.py</files>
  <behavior>
    - test_planner_delta_mode_input: When state.gap_report is not None, user_message passed to call_agent contains "Gap report:" as the FIRST section and "Spec (reference only):" later. Verify via mock args.
    - test_planner_delta_mode_output: When state.gap_report is not None, run_planner returns a PlannerOutput (mocked with fewer tasks than full plan).
    - test_planner_delta_includes_completed_ids: When state.task_graph has tasks with status "completed", the user_message contains "Completed task IDs:" with those IDs. Verify via mock.
    - test_planner_delta_no_completed_ids_on_empty_graph: When state.task_graph is None but gap_report exists, user_message contains "Completed task IDs: []". No crash.
    - test_planner_delta_system_prompt_differs: The system prompt for Mode B is different from Mode A. Capture the system arg from mock in both modes and assert they differ.
  </behavior>
  <action>
    RED: Add 5 new tests to tests/test_planner.py. Build a delta-mode BuildState fixture: spec=simple dict, gap_report={"gaps": [{"feature": "user-model", "description": "missing"}]}, task_graph=[{"id": "task-create-user-model", "status": "completed"}], iteration=1. Run tests — all must FAIL.

    GREEN: Add to declarative_ai/agents/planner.py:
    - PLANNER_SYSTEM_PROMPT_DELTA: System prompt for Mode B. Must instruct: "You are performing a delta re-plan. Produce ONLY tasks that address the provided gap report. Do NOT generate tasks for items absent from the gap report. Items not in the gap report are already correct."
    - _get_completed_task_ids(task_graph: list[dict] | None) -> list[str]: Extract IDs where status == "completed". Return [] if task_graph is None.
    - Update run_planner to branch on state.gap_report:
      - If not None (Mode B): build user message with gap report first, completed IDs, then spec as reference. Use PLANNER_SYSTEM_PROMPT_DELTA.

    Run tests — all must PASS.

    REFACTOR: Extract _build_user_message_delta(spec, gap_report, completed_ids) helper. Add docstrings referencing PLAN-05.
  </action>
  <verify>
    <automated>cd C:/Users/User/Desktop/declerative-ai && python -m pytest tests/test_planner.py -x -v</automated>
  </verify>
  <done>run_planner handles both Mode A and Mode B, delta mode puts gap report first and spec as reference only, completed task IDs extracted from task_graph — all 11 planner tests green</done>
</task>

</tasks>

<verification>
Run full Phase 2 test suite:
```
cd C:/Users/User/Desktop/declerative-ai && python -m pytest tests/test_task_schema.py tests/test_task_graph.py tests/test_planner.py -v
```
Run full project suite to confirm no regressions:
```
cd C:/Users/User/Desktop/declerative-ai && python -m pytest tests/ -x
```
</verification>

<success_criteria>
- run_planner importable from declarative_ai.agents.planner
- Mode A (first iteration): full spec decomposition prompt, returns PlannerOutput
- Mode B (delta): gap report as primary input, spec as reference, completed IDs included
- All 11 planner tests pass
- Full project test suite green (Phase 1 + Phase 2 schemas + graph + planner)
</success_criteria>

<output>
After completion, create `.planning/phases/02-planner-agent/02-02-SUMMARY.md`
</output>
